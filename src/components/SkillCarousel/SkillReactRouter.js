import React from "react";

export default function SkillBootstrap(props) {
  return (
    <div
      className="card h-full flex flex-col justify-center"
      style={{ background: props.background }}
    >
      React-Router
    </div>
  );
}
