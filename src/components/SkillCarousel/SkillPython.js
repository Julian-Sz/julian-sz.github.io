import React from "react";

export default function SkillPython(props) {
  return (
    <div
      className="card h-full flex flex-col justify-center"
      style={{ background: props.background }}
    >
      Python
    </div>
  );
}
